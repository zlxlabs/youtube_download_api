# 架构概览

本文档介绍 YouTube Audio API 的整体架构设计。

## 目录

- [架构图](#架构图)
- [核心组件](#核心组件)
- [数据流程](#数据流程)
- [技术栈](#技术栈)
- [设计原则](#设计原则)

---

## 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端                                   │
│  - Web UI / Mobile App / 第三方集成                               │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/HTTPS
                             │ REST API
┌────────────────────────────▼────────────────────────────────────┐
│                      FastAPI 应用层                              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API Routes                                               │  │
│  │  - /api/v1/tasks          任务管理                        │  │
│  │  - /api/v1/files          文件下载                        │  │
│  │  - /api/v1/manual-upload  人工上传                        │  │
│  │  - /api/v1/video-resources 视频资源                      │  │
│  │  - /api/v1/settings       系统设置                        │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  业务服务层 (Services)                                     │  │
│  │  - TaskService          任务管理                          │  │
│  │  - FileService          文件管理                          │  │
│  │  - CallbackService      Webhook 回调                      │  │
│  │  - NotifyService        通知服务（企微）                   │  │
│  │  - ManualUploadService 人工上传服务                       │  │
│  │  - MetadataService      元数据服务                        │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  核心层 (Core)                                             │  │
│  │  - DownloaderManager   下载器管理器                      │  │
│  │  - Worker              异步任务 Worker                     │  │
│  │  - IPBanBreaker        IP 熔断器                          │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  下载器层 (Downloaders)                                   │  │
│  │  - CDPDownloader       Chrome DevTools Protocol          │  │
│  │  - YtdlpDownloader     yt-dlp 本地下载器                  │  │
│  │  - TikHubDownloader    TikHub API 服务                   │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  数据访问层 (DB)                                           │  │
│  │  - SQLite Database     数据库                            │  │
│  │  - VideoResources      视频资源表                        │  │
│  │  - Files               文件表                             │  │
│  │  - Tasks               任务表                             │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │  文件存储层 (Storage)                                      │  │
│  │  - /data/files/audio/     音频文件                        │  │
│  │  - /data/files/transcript/ 字幕文件                     │  │
│  │  - /data/logs/            日志文件                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

外部依赖：
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ YouTube API     │  │ TikHub API     │  │ Chrome CDP      │
│ (元数据/下载)    │  │ (下载服务)      │  │ (真实浏览器)     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ PO Token        │  │ 企业微信        │  │ 代理服务         │
│ (风控绕过)       │  │ (通知)          │  │ (开发环境)       │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

---

## 核心组件

### 1. API Routes 层

**职责**：
- 处理 HTTP 请求和响应
- 请求参数验证
- 鉴权和权限控制
- 调用业务服务层

**主要模块**：
- `routes.py` - 主要 API 路由
- `settings_routes.py` - 设置相关路由
- `manual_upload_routes.py` - 人工上传路由
- `video_resource_routes.py` - 视频资源路由

---

### 2. Services 层

**职责**：
- 实现业务逻辑
- 协调多个核心组件
- 数据转换和验证

**主要服务**：
- `TaskService` - 任务创建、查询、取消
- `FileService` - 文件下载、清理
- `CallbackService` - Webhook 回调
- `NotifyService` - 企业微信通知
- `ManualUploadService` - 人工上传处理
- `MetadataService` - 视频元数据获取

---

### 3. Core 层

**职责**：
- 核心业务逻辑
- 下载器管理
- 异步任务处理

**主要组件**：
- `DownloaderManager` - 下载器管理和降级。**全局单例**：由 `main.py` 的 `lifespan` 统一创建一次，注入给 Worker、TaskService（用于任务创建前的前置检查）、NotificationService 等，避免出现多份互相独立的熔断器/统计状态
- `Worker` - 异步任务队列
- `IPBanBreaker` - IP 熔断器保护

---

### 4. Downloaders 层

**职责**：
- 实现不同下载器
- 统一的下载接口
- 下载器降级和熔断

**下载器**：
- `CDPDownloader` - Chrome DevTools Protocol 下载器
- `YtdlpDownloader` - yt-dlp 本地下载器
- `TikHubDownloader` - TikHub API 服务

---

### 5. Database 层

**职责**：
- 数据持久化
- 数据查询和更新
- 缓存管理

**数据表**：
- `video_resources` - 视频资源（元数据缓存）
- `files` - 文件记录
- `tasks` - 任务记录
- `manual_uploads` - 人工上传记录

---

## 数据流程

### 下载任务流程

```
1. 客户端创建任务
   POST /api/v1/tasks
   ↓
2. TaskService 检查文件级缓存（该 video_id 下是否已有请求的音频/字幕文件）
   ├─ 命中且已覆盖请求 → 直接返回（cache_hit=true）
   └─ 未命中/覆盖不足 → 继续
   ↓
3. 检查同 video_id 是否有活跃（pending/downloading）任务，避免重复创建
   ├─ 有且已覆盖本次请求的音频/字幕需求 → 复用返回该任务
   └─ 无，或覆盖不足 → 继续
   ↓
4. 前置检查（precheck）：探测视频是否已知不可下载（直播/预约首播/私享/地区限制等）
   ├─ 明确判定不可下载 → 同步返回 422，不创建任务
   └─ 探测超时/下载器全部失败/其它异常 → fail-open，继续
   ↓
5. TaskService 创建任务记录，加入队列
   ↓
6. Worker 从队列取出任务执行
   ↓
7. DownloaderManager 按场景化优先级选择下载器
   ├─ CDP（真实浏览器指纹，音频场景最高优先级）
   ├─ yt-dlp
   └─ TikHub
   ↓
8. 下载器下载资源
   ├─ 音频文件 → /data/files/audio/
   └─ 字幕文件 → /data/files/transcript/
   ↓
9. FileService 保存文件记录
   ↓
10. VideoResources 更新缓存
   ↓
11. CallbackService 发送 Webhook（如配置）
   ↓
12. NotifyService 发送企微通知（如配置）
```

---

## 技术栈

### 后端框架

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11+ | 主要编程语言 |
| FastAPI | 最新 | Web 框架 |
| uv | 最新 | 包管理器 |
| Pydantic | 最新 | 数据验证 |

### 数据库

| 技术 | 版本 | 用途 |
|------|------|------|
| SQLite | 3.x | 数据库（生产环境可升级到 PostgreSQL） |

### 下载工具

| 技术 | 版本 | 用途 |
|------|------|------|
| yt-dlp | 最新 | YouTube 下载 |
| Playwright | 最新 | 浏览器自动化 |
| curl-cffi | 最新 | TLS 指纹模拟 |

### 其他工具

| 技术 | 版本 | 用途 |
|------|------|------|
| Docker | 20.10+ | 容器化部署 |
| Docker Compose | 2.0+ | 多容器编排 |
| Nginx | 1.24+ | 反向代理（可选） |

---

## 设计原则

### 1. 低耦合、高内聚

- 各模块职责单一
- 模块间通过接口通信
- 易于测试和维护

### 2. 可扩展性

- 支持添加新下载器
- 支持多种通知方式
- 支持多种存储后端

### 3. 容错性

- 下载器降级机制
- 熔断器保护
- 任务重试机制

### 4. 缓存优化

- 元数据永久缓存
- 文件级缓存复用
- API 响应缓存（TikHub）

### 5. 风控应对

- IP 熔断器保护
- CDP 真实浏览器指纹
- Cookie 管理和协同
- PO Token 支持

---

## 相关文档

- [下载器架构详解](./downloader-architecture.md)
- [配置总览](../configuration/overview.md)
- [部署指南](../deployment.md)
