# ops.env.example —— 可观测变量注入模板(GlitchTip)。复制为 ops.env 放 n305 部署目录填真实值。
# ops.env 不进 git、不被挂载,只经 compose env_file 注入容器 os.environ。
# DSN 取值见 ops-dispatcher deploy/monitoring/dsn-output.txt 对应条目。
SENTRY_DSN=https://<public_key>@100.107.95.24:9000/<project_id>
